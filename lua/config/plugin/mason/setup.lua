require("mason").setup({
  ui = {
    border = "single",
    width = 0.8,
    height = 0.8,
  },
  registries = {
    "github:nvim-java/mason-registry",
    "github:mason-org/mason-registry",
  },
})
